/**
 * Post-signup onboarding: username → profile → first session type → availability
 *
 * TODO: Implement (subagent task)
 */
export default function OnboardingPage() {
  return <div>OnboardingPage placeholder</div>;
}
